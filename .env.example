# Example environment variables for local development
# Copy this file to .env and fill in the values if needed.

# FLASK_DEBUG=1 # Uncomment for Flask debug mode (Not recommended for production)
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/keyfile.json" # Optional: Path to service account key file for local Firestore access
# GOOGLE_CLOUD_PROJECT="your-gcp-project-id" # Optional: Specify project ID if not using default
